import { View } from "react-native";
import { FormField } from "./FormField";

export function Input({
  label,
  name,
  required,
  placeholder,
  inputType = "text",
  icon,
  style = " ring-1 ",
  value,
  tooltip,
  subfix,
  ...props
}: FormField) {
  return <View>
      
  </View>;
}
